# FastAPI and ASGI server
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Database
sqlalchemy[asyncio]>=2.0.36
aiosqlite>=0.20.0
alembic>=1.14.0

# Settings and validation
# Python 3.13 requires pydantic 2.10+ for pre-built wheels
pydantic>=2.10.0
pydantic-settings>=2.6.0

# Logging
structlog>=24.4.0

# Security and rate limiting
slowapi>=0.1.9

# System monitoring
psutil>=6.1.0

# HTTP client (for testing/webhooks)
httpx>=0.28.0

# Camera support
# Note: picamera2 should be installed via system package (python3-picamera2)
# to avoid lengthy PyAV compilation. The pip version is kept commented for reference.
# picamera2==0.3.17
# Note: opencv-python-headless removed - not used in current implementation
# Can be added later if image processing features are needed
